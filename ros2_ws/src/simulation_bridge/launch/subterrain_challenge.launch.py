#!/usr/bin/env python3

from launch import LaunchDescription


def generate_launch_description():
    # TODO: full mission launch (includes simulation, perception, mapping,
    #       path_planner, trajectory_planner, quadrotor_controller, state_machine)
    return LaunchDescription([])
