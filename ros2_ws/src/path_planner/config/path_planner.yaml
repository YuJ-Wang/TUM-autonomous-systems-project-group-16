path_planner_node:
  ros__parameters:
    map_resolution: 0.15        # must match perception octomap resolution
    safety_margin: 0.5          # inflate obstacles by this [m]
    max_expansions: 100000      # A* budget
    waypoint_step: 3            # keep every N-th voxel in output path
